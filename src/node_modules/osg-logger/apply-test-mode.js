var config = 
    { doNotReplaceConsole : false
    , levels      :
        { "[all]"      : "WARN"
        }
    , appenders   :
      [ { type    : "console"
        }
      ]
    }
  ;

try {
    config = require( process.cwd() + "/test/loggers" );
}catch (ex){}

require('./').configure( config );
